import { heroSectionCarousel } from './frontend/homeCarousel.js';
import { toggleMenu } from './frontend/menuToggle.js'

document.addEventListener("DOMContentLoaded", () => {
    heroSectionCarousel();
    toggleMenu();
});